# In dev, leave this unset — Vite proxies /api to http://localhost:5050.
# In production (Vercel), set it to the deployed backend URL, including /api.
# Example:
# VITE_API_URL=https://taskflow-api.onrender.com/api
